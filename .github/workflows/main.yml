name: Xcode - Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: List Files
        run: ls -R
      
      - name: Install Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 15

      - name: Build for testing
        run: |
          xcodebuild build-for-testing \
          -project Código/FPSARMini06/FPSARMini06.xcodeproj \
          -scheme FPSARMini06 \
          -destination 'platform=iOS Simulator,name=iPhone 15 Pro'
